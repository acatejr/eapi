version: '3.9'

services:
    db:
        image: postgres:alpine
        env_file: .env
        ports:
            - 5432:5432
    adminer:
        image: adminer
        # restart: always
        ports:
            - 8080:8080
